services:
  model-atlas:
    image: eclipsefennec/model.atlas:jena-snapshot
    stdin_open: true
    tty: true

    ports:
      - "8080:8080"
    environment:
      - STORAGE_ROOT=/opt/modelatlas/runtime/data
      - ATLAS_HTTP_PORT=8080
    volumes:
      - ./org.eclipse.fennec.model.atlas.runtime.config.local.jena.jar:/opt/modelatlas/runtime/load/jena-config.jar
      - atlas-data:/opt/modelatlas/runtime/data
      - atlas-log:/opt/modelatlas/runtime/log

volumes:
  atlas-data:
  atlas-log:
